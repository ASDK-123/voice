# P0 回归记录（UI/UX 计划书 6.1）

日期：2026-02-17  
对应计划：`docs/UI_UX_P0_P1_P2_详细计划书.md`（第 6.1 节）  
执行人：Codex

## 1. 回归范围

- P0-WP1：token 补齐
- P0-WP2：主流程去 Emoji
- P0-WP3：硬编码颜色替换
- P0-WP4：轻量交互一致性（关键按钮防重入）

## 2. 本次执行方式

- 自动回归：
  - `python -m pytest tests/ -q`（17 passed）
  - `python scripts/m4_final_acceptance_test.py`（OK）
  - `python scripts/p2_backend_acceptance_test.py`（OK）
- UI 构造 smoke（offscreen）：
  - `scripts/smoke_wizard_construct.py`（OK）
  - `CosyVoiceProApp` 主窗口构造（使用 `.pixi/envs/default/python.exe`，OK）

## 3. 6.1 手工回归矩阵执行结果

1. 页面打开与导航：通过（smoke）
- 结果：`CosyVoiceProApp` 五个主页面接口均成功构造（`text/task/voice/api/settings`）。
- 说明：在 offscreen 环境执行，未进行人工点击。

2. 关键流程
- 文本编辑 -> 一键运行：部分通过（代码路径可达，模型依赖流程待人工）
- 文本编辑 -> 转任务计划 -> 全部运行：部分通过（入口与运行态防重入逻辑通过）
- 语音设置（加载/保存/应用/编译）：待人工
- refs 面板（上传/绑定/解绑/试听/备注）：待人工
- API 页（启动/停止 API，启动/停止桥接，刷新 voices）：部分通过（UI 逻辑与状态联动已验证，真实启停待人工）

3. 主题切换（Light/Dark）文本可读：待人工
- 说明：本次仅做代码级与 offscreen smoke，未进行可视化对比截图。

4. 窗口宽度（1024、1280、1400+）：待人工
- 说明：需要真实桌面分辨率下观察布局稳定性。

## 4. P0 结果判定

1. `P0-WP1/WP2/WP3`：完成
- 证据：
  - emoji 检查命中为 0（`ui/`）
  - `gray` / `rgba(0,0,0,0.65)` 命中为 0（`ui/`）
  - `voice_settings.py` 最后一处硬编码颜色已替换为 token

2. `P0-WP4`：完成（代码层）
- 证据：关键异步按钮（API 启停、桥接启停、任务生成）均具备禁用/恢复逻辑。

## 5. 遗留与下一步

1. 需要你本机做一轮可视化人工点测（重点：第 3 节中“待人工”项）。
2. P1-WP1 已启动并落地首版：`api_page.py` 已重排为  
`服务控制 -> 运行开关 -> voices 列表 -> 日志`，并新增日志等级徽标与 API/桥接状态卡片。

